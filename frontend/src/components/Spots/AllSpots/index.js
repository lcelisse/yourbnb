import { useDispatch } from "react-redux";

export default function AllSpots() {
  const dispatch = useDispatch();

  return (
    <div>
      <div>
        <h1>hi</h1>
      </div>
    </div>
  );
}
